# PaperFate 야간 진척 — 2026-05-26 아침 보고

> 사용자가 자는 동안 메인 (Claude) 자율 작업. 코덱스 Round 6은 활동 없음
> (Q500 fulltext batch는 비용 estimate 단계에서 stop signal 인 듯).

## Production에 새로 라이브 (paperfate.com)

| Endpoint | 변경 | 라이브 검증 |
|---|---|---|
| `/api/forecast` | + `review_timeline_days` (v0.4-timeline), + `counterfactual_suggestions`, journey의 `switchCostValue` 숫자 노출 | ✓ |
| `/api/similar` | 800 → 2,840 journal JIF lookup | ✓ |
| `/api/journals-search` | 800 → 2,840 journal autocomplete | ✓ |
| `/api/journal-info` | **신규** — 단일 저널 prior-year IF lookup (`?issn=` or `?name=`) | ✓ |
| `/api/references` | **신규** — DOI list (≤50) → median/mean IF, top venues, JCR cat 분포 | ✓ |

Production smoke (`scripts/smoke-production.mjs`) **13/13 PASS**.

## UI 새 카드 / 입력

- **Target journal 입력 textbox** — 사용자가 저널 이름/ISSN 입력 시 "If you submit to NEJM, prior-year IF 78.5" 카드 표시.
- **Reference DOIs textarea** — 사용자가 reference DOI 리스트 입력 시 "Your bibliography: 5/5 resolved, median IF 176" 카드 표시.
- **Methods 섹션** (about / FAQ 사이) — corpus, pre-pub feature 정책, conformal calibration, limitations.
- **Confidence + model version 배지** — ResultPanel 푸터에 confidence %, fatecore version, timeline version chip.
- **Switch-cost 숫자 노출** — journey 카드 "switch cost · moderate (0.50)" 표시 + 4단계 색상 (emerald/fate/amber/rose).
- **4 sample manuscript 버튼** — RCT cardio / Meta oncology / Cohort pediatrics / AI radiology.

## 모델 / 데이터 진척

- **`weights/journals-shortlist.json`** — 800 → **2,840 journals** (latest-year JCR JIF + OpenAlex meta 조인). tier 분포: top 38 / high 202 / upper_mid 500 / mid 900 / lower_mid 700 / low 500.
- **Journey field-matching fix** — sanity-samples로 발견한 두 버그 수정:
  - Pediatric-asthma cohort → OBGYN으로 잘못 routing되던 것 ('maternal' 키워드 obgyn에서 제거).
  - CKD/SGLT2 RCT → Endourology로 잘못 routing되던 것 ('urology' nephrology 매핑에서 제거).
  - `scoreFit`의 fieldBoost를 3-tier (2+ overlap = 3.0x, 1 = 2.2x, mismatch = 0.55x penalty)로 강화.
  - 검증: Cohort pediatrics journey가 **Pediatric Nursing → Pediatric Cardiology → Clinical Pediatrics**로 정상화.

## 인프라 / SEO

- **`.vercelignore`** — archived weights를 deploy bundle에서 제외 (Vercel 250MB 한도 보호).
- **OpenGraph + Twitter card + sitemap.xml + robots.txt + og.svg** — 사이트 link preview / 검색 노출 강화.
- **`scripts/smoke-production.mjs`** — 5 endpoint 13 check 자동화.
- **`scripts/sanity-samples.mjs`** — 4 sample manuscript 횡단 sanity check (journey + cost + suggestion + timeline).
- **`scripts/build-journals-shortlist-v2.mjs`** — DB에서 shortlist 재빌드 (idempotent, 새 JCR year 들어올 때마다 재실행).

## 추가 진행 (round 2)

- **Suggestion engine v2** — top-3 weakest items joint counterfactual. `joint_counterfactual` field 추가 — items_count, predicted_jif_lift, sum_singletons, **interaction_gap** (super-/sub-additive 표시), baseline/lifted JIF. ResultPanel에 "Fix all top 3 → +X JIF together" 카드 + green/amber chip.
- **Bibliography vs manuscript JIF consistency chip** — references 입력 시 manuscript JIF point와 ref median IF 비교. ratio [0.5–2.0] emerald 'matched' / <0.5 fate 'stretch worth trying' / >2.0 amber 'broader-impact framing'.
- **Production smoke + sanity 최종**: 13/13 + 4/4 모두 PASS.

## 야간 commit 목록 (이번 자율 session)

```
782bb10 feat(ui): bibliography vs manuscript JIF consistency chip
a204978 feat(suggestions): joint counterfactual for top-3 weakest items
4b1a69c docs: morning summary — EPMC ingest + corpus growth
5bd14a2 docs(codex): Round 6.5 cost-free brief
9e74c25 feat(ui): tag timeline card with the active model badge
0c3a0c7 docs: morning summary of overnight progress
0b7df72 docs: README with API surface + models table + pre-pub policy
4933e30 fix(journey): drop 'maternal' from obgyn, broaden pulmo/peds
1932057 fix(journey): detectFields hardening v1 + sanity-samples script
1c76324 feat(api): /api/journal-info
9bf0c21 feat(ui): target-journal input + prior-year IF card
224ce58 feat(ui): Methods section
b8c4507 feat(api): /api/references
b8036a3 feat(ui): reference DOI textarea + bibliography card
20bd9e5 feat(journals): 800 → 2,840 shortlist
28143e1 test(smoke): extend to journal-info + references
26b5605 seo: OpenGraph / Twitter / sitemap / robots
0de4f36 seo: OG image + share card
b8a6873 feat(ui): confidence + model version badge
```

## 코덱스 Round 5 결과 (저녁에 받은 것, 이미 production wired)

- **v0.4-timeline DEPLOY CANDIDATE** — MAE 59.4d, R²(log) 0.13, 90.2% conformal coverage. `predictions.review_timeline_days` 라이브.
- **v0.3-pub HOLD 확정** — `j_hist_jcr_jif` shuffle 시 R² 0.94 → -0.75. Trivial ISSN autocorrelation. 학습 모델로 deploy 안 함. Target-aware UX는 `/api/journal-info` 직접 lookup로.

## DB 진척

| Field | 이전 (저녁) | 지금 |
|---|---:|---:|
| Total papers | 3,501,539 | **3,897,021** (+395K) |
| `with_epmc_fulltext` | 210,855 | **416,924** (+206K) |
| `with_pmc_fulltext` | 461,353 | 461,353 |
| `with_pdf_fulltext` | 6,411 | 7,118 |
| Any fulltext | ~530K (15%) | **~730K (~19%)** |
| `review_days_received_to_accepted` | 0 | **2,047,937** (53%) |
| DB size | 83,156 MB | 85,300 MB (+2.1GB) |

EPMC manual ingest 도중에 5.6분, watcher 무시되던 누적분이 DB에 들어감.

## Background 채널 상태

- 🔥 **EPMC fulltext PMCID-wide** (PID 72296) — 254K/772K (33%), ok ~205K (히트 누적 둔화: PMID expansion bucket이 OA 외 영역으로 진입). 다 돌면 약 10-15K 추가 회수만 예상.
- 🐢 **PDF fulltext** (PID 74988) — publishers 차단 누적, marginal.
- 🌱 **PubMed year-balanced** (PID 46304) — 신규 metadata 계속.
- 🛠 **Auto-ingest watcher** (PID 61872) — 가동 중. 단 EPMC ingest는 `changed:false`로 skip되고 있어 manual `node scripts/build-unified-db.mjs --only=epmc` 한 번 trigger (PID `bafl7wmsf`).
- ✅ OpenAlex enricher / references / Crossref / iCite / S2 / Unpaywall / NIH RePORTER — 모두 완료 상태.

## 코덱스 Round 6에 남은 작업 (사용자 동의 후 진행 가능)

1. **Q500 fulltext rescoring batch** (~$80-100, Gemini Flash) — PMC body 530K + EPMC body 누적분으로 본문 의존 Q500 items 재채점, paper_scores 두 배.
2. **paper_references DB-backed cold-start features** — 6M edges로 ref-list 입력 빠른 lookup (OpenAlex 기반 메인 `/api/references`는 이미 라이브, DB-backed은 v0.5 학습 input용).
3. **journals-shortlist 3,000+ 확장** — 메인이 2,840으로 이미 확장. ✓ done.

## 다음 phase 권고

- v0.5 학습은 새 EPMC body가 충분히 누적되면 시작. 단 본문은 post-publication signal이라 학습 시 사용 금지 — Q500 본문 채점 결과 (Gemini scoring)는 OK.
- `review_timeline_days` UI는 이미 live이지만 ResultPanel timeline 카드의 note copy를 model 강조하도록 v0.4 badge 더 명확하게 가능.
- Suggestion engine은 weakest item별 단일 counterfactual. 멀티-item joint counterfactual은 더 큰 lift 추정 가능 (Round 7 후보).

— Claude
